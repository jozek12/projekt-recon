movies:
    movie_id <- int primary key
    averageScore <- int
    seasonYear <- int
    description <-text
movie_genres:
    movie_id <-\
                = compose key for tables movies and genres_list
    genre_id <-/
genres_list:
    genre_id <- int primary key
    name <- text
movies_translations:
    movie_id <- int foreign key to movies.id
    title <- text
    language <- text
